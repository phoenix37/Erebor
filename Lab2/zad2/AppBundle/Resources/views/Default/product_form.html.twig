{% extends '::base.html.twig' %}
{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    <a href={{ path('product_demo_homepage') }}>Cancel</a>
{% endblock %}